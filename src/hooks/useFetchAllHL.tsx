import { useEffect, useState } from "react";

export default function useFetchAllHL() {
  const [allHeadlines, setAllHeadlines] = useState([]);

  useEffect(() => {
    // code
  }, []);

  return;
  {
    allHeadlines;
  }
}
